title: TinyCAD Manual
description: User and developer documentation for TinyCAD
theme: minima
markdown: kramdown
permalink: pretty

plugins:
  - jekyll-feed
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default